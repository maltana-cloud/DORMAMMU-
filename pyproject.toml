[project]
name = "dormammu"
version = "0.1.0"
description = "DORMAMMU autonomous intelligence and ecosystem platform"
requires-python = ">=3.11"
dependencies = []

[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"
